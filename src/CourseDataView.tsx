import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'

type Props = { data: Record<string, unknown> }
export default function CourseDataView({ data }: Props) { const table = data.table as { headers?: string[]; rows?: string[][] } | undefined; const chart = data.chart as { labels?: string[]; values?: number[] } | undefined; return <>{table?.headers && table.rows && <div className="course-data-table"><table><thead><tr>{table.headers.map((header) => <th key={header}>{header}</th>)}</tr></thead><tbody>{table.rows.map((row, index) => <tr key={index}>{row.map((cell, cellIndex) => <td key={cellIndex}>{cell}</td>)}</tr>)}</tbody></table></div>}{chart?.labels && chart.values && <div className="course-data-chart"><ResponsiveContainer width="100%" height={210}><BarChart data={chart.labels.map((label, index) => ({ label, value: chart.values?.[index] ?? 0 }))}><CartesianGrid stroke="#e3e4dc" strokeDasharray="3 3" vertical={false} /><XAxis dataKey="label" /><YAxis /><Tooltip /><Bar dataKey="value" fill="#527ba0" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer></div>}</> }
